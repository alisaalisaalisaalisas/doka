/* Global Playground: scenarios-global-python.js — enriched with filesystem */
S("Python","gc-py-1","argparse: required arg missing","Junior",`<h3>Контекст</h3><p>Python: <b>argparse: required arg missing</b>. Работа с <code>app.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>argparse: required arg missing</b>. Файл <code>app.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>app.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>app.py</code>. Активный: <code>app.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat app.py</pre>`,"dev@py:~$",[["^python3 app.py --help | head -20",`error or missing output`,"err"],["^python3 app.py 2>&1 | grep -i error | head -5",`check output`,"warn"],["^sed -i s/required=False/required=True/ app.py",`fixed`,"ok"],["^python3 app.py --arg val | head -5",`ok verified`,"ok"]],[{re:"^python3 app.py --help | head -20",l:"диагностика"},{re:"^sed -i s/required=False/required=True/ app.py",l:"исправить"}],{file:"app.py",files:{"app.py":`import argparse\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--input\", required=False)  # BUG\n`},checks:[{re:/required\s*=\s*True/,l:"required=True"}],solutionFiles:{"app.py":`import argparse\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--input\", required=True)\n`}} ,{hints:["Симптом: argparse: required arg missing в app.py. Ищи причину в этом файле.","Открой app.py в редакторе. Начни с cat app.py.","диагностика → исправить"]});
S("Python","gc-py-2","pathlib: Path vs str join bug","Middle",`<h3>Контекст</h3><p>Python: <b>pathlib: Path vs str join bug</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>pathlib: Path vs str join bug</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",`error or missing output`,"warn"],["^python3 -c \"import pathlib; help(pathlib.Path.joinpath)\" | head -20",`check output`,"warn"],["^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",`fixed`,"ok"],["^python3 app.py && ls out | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",l:"диагностика"},{re:"^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# pathlib: Path vs str join bug\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# pathlib: Path vs str join bug — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: pathlib: Path vs str join bug в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-3","subprocess: shell=True injection risk","Senior",`<h3>Контекст</h3><p>Python: <b>subprocess: shell=True injection risk</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>subprocess: shell=True injection risk</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import subprocess; help(subprocess.run)\" | grep -A2 timeout | head -10",`check output`,"warn"],["^sed -i s/shell=True/shell=False/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/shell=True/shell=False/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# subprocess: shell=True injection risk\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# subprocess: shell=True injection risk — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: subprocess: shell=True injection risk в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-4","subprocess: timeout 30s not handled","Junior",`<h3>Контекст</h3><p>Python: <b>subprocess: timeout 30s not handled</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>subprocess: timeout 30s not handled</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import subprocess; help(subprocess.run)\" | grep -A2 timeout | head -10",`check output`,"warn"],["^sed -i s/shell=True/shell=False/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/shell=True/shell=False/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# subprocess: timeout 30s not handled\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# subprocess: timeout 30s not handled — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: subprocess: timeout 30s not handled в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-5","socket: SO_REUSEADDR not set, bind fails","Middle",`<h3>Контекст</h3><p>Python: <b>socket: SO_REUSEADDR not set, bind fails</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>socket: SO_REUSEADDR not set, bind fails</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",`error or missing output`,"err"],["^python3 -c \"import socket; print(socket.SO_REUSEADDR)\"",`check output`,"warn"],["^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",`fixed`,"ok"],["^python3 server.py & sleep 1; curl -s localhost:8080/healthz | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",l:"диагностика"},{re:"^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# socket: SO_REUSEADDR not set, bind fails\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# socket: SO_REUSEADDR not set, bind fails — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: socket: SO_REUSEADDR not set, bind fails в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-6","socket: timeout not set, hangs","Senior",`<h3>Контекст</h3><p>Python: <b>socket: timeout not set, hangs</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>socket: timeout not set, hangs</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import socket; print(socket.SO_REUSEADDR)\"",`check output`,"warn"],["^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",`fixed`,"ok"],["^python3 server.py & sleep 1; curl -s localhost:8080/healthz | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",l:"диагностика"},{re:"^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# socket: timeout not set, hangs\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# socket: timeout not set, hangs — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: socket: timeout not set, hangs в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-7","requests: Session not reused, 100ms overhead","Junior",`<h3>Контекст</h3><p>Python: <b>requests: Session not reused, 100ms overhead</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>requests: Session not reused, 100ms overhead</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import requests; s=requests.Session(); print(s)\" | head -5",`check output`,"warn"],["^sed -i s/requests.get/requests.Session().get/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i retry | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/requests.get/requests.Session().get/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# requests: Session not reused, 100ms overhead\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# requests: Session not reused, 100ms overhead — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: requests: Session not reused, 100ms overhead в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-8","requests: retry not configured, flaky 503","Middle",`<h3>Контекст</h3><p>Python: <b>requests: retry not configured, flaky 503</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>requests: retry not configured, flaky 503</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import requests; s=requests.Session(); print(s)\" | head -5",`check output`,"warn"],["^sed -i s/requests.get/requests.Session().get/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i retry | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/requests.get/requests.Session().get/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# requests: retry not configured, flaky 503\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# requests: retry not configured, flaky 503 — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: requests: retry not configured, flaky 503 в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-9","asyncio: event loop already running","Senior",`<h3>Контекст</h3><p>Python: <b>asyncio: event loop already running</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: event loop already running</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: event loop already running\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: event loop already running — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: event loop already running в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-10","asyncio: gather vs wait, exception swallowed","Junior",`<h3>Контекст</h3><p>Python: <b>asyncio: gather vs wait, exception swallowed</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: gather vs wait, exception swallowed</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: gather vs wait, exception swallowed\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: gather vs wait, exception swallowed — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: gather vs wait, exception swallowed в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-11","threading: race condition, need Lock","Middle",`<h3>Контекст</h3><p>Python: <b>threading: race condition, need Lock</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>threading: race condition, need Lock</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; print(threading.active_count())\" | head -5",`error or missing output`,"warn"],["^python3 app.py 2>&1 | grep -i race | head -10",`check output`,"warn"],["^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i ok | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; print(threading.active_count())\" | head -5",l:"диагностика"},{re:"^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# threading: race condition, need Lock\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# threading: race condition, need Lock — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: threading: race condition, need Lock в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-12","threading: daemon thread not joined","Senior",`<h3>Контекст</h3><p>Python: <b>threading: daemon thread not joined</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>threading: daemon thread not joined</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; print(threading.active_count())\" | head -5",`error or missing output`,"warn"],["^python3 app.py 2>&1 | grep -i race | head -10",`check output`,"warn"],["^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i ok | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; print(threading.active_count())\" | head -5",l:"диагностика"},{re:"^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# threading: daemon thread not joined\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# threading: daemon thread not joined — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: threading: daemon thread not joined в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-13","GIL: cpu bound 4x slower in threads","Junior",`<h3>Контекст</h3><p>Python: <b>GIL: cpu bound 4x slower in threads</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>GIL: cpu bound 4x slower in threads</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; help(threading)\" | grep -i gil | head -5",`error or missing output`,"warn"],["^python3 bench.py 2>&1 | grep -A2 time | head -10",`check output`,"warn"],["^python3 -c \"from multiprocessing import Pool; Pool(4).map(func, range(100))\" 2>&1 | head -10",`fixed`,"ok"],["^python3 bench.py | tail -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; help(threading)\" | grep -i gil | head -5",l:"диагностика"},{re:"^python3 -c \"from multiprocessing import Pool; Pool(4).map(func, range(100))\" 2>&1 | head -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# GIL: cpu bound 4x slower in threads\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# GIL: cpu bound 4x slower in threads — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: GIL: cpu bound 4x slower in threads в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-14","tracemalloc: найти утечку 200MB","Middle",`<h3>Контекст</h3><p>Python: <b>tracemalloc: найти утечку 200MB</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>tracemalloc: найти утечку 200MB</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -X tracemalloc app.py 2>&1 | grep -A5 tracemalloc | head -20",`error or missing output`,"warn"],["^python3 -c \"import tracemalloc; tracemalloc.start(); snap=tracemalloc.take_snapshot(); print(snap.statistics('lineno')[:3])\" | head -10",`check output`,"warn"],["^sed -i s/\"leak\"/\"fixed\"/ app.py",`fixed`,"ok"],["^python3 -X tracemalloc app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -X tracemalloc app.py 2>&1 | grep -A5 tracemalloc | head -20",l:"диагностика"},{re:"^sed -i s/\"leak\"/\"fixed\"/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# tracemalloc: найти утечку 200MB\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# tracemalloc: найти утечку 200MB — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: tracemalloc: найти утечку 200MB в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-15","cProfile: hot spot 80% в json.loads","Senior",`<h3>Контекст</h3><p>Python: <b>cProfile: hot spot 80% в json.loads</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>cProfile: hot spot 80% в json.loads</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -m cProfile -s cumulative app.py 2>&1 | head -30",`error or missing output`,"warn"],["^python3 -m pstats app.prof 2>&1 | head -20",`check output`,"warn"],["^python3 -m cProfile -o app.prof app.py && python3 -c \"import pstats; p=pstats.Stats('app.prof'); p.sort_stats('cumulative').print_stats(5)\" | head -20",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i time | head -5",`ok verified`,"ok"]],[{re:"^python3 -m cProfile -s cumulative app.py 2>&1 | head -30",l:"диагностика"},{re:"^python3 -m cProfile -o app.prof app.py && python3 -c \"import pstats; p=pstats.Stats('app.prof'); p.sort_stats('cumulative').print_stats(5)\" | head -20",l:"исправить"}],{file:"main.py",files:{"main.py":`# cProfile: hot spot 80% в json.loads\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# cProfile: hot spot 80% в json.loads — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: cProfile: hot spot 80% в json.loads в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-16","logging: basicConfig called twice, handler dup","Junior",`<h3>Контекст</h3><p>Python: <b>logging: basicConfig called twice, handler dup</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>logging: basicConfig called twice, handler dup</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",`error or missing output`,"warn"],["^cat app.py | grep -A2 logging | head -20",`check output`,"warn"],["^sed -i s/basicConfig/dictConfig/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | head -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/basicConfig/dictConfig/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# logging: basicConfig called twice, handler dup\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# logging: basicConfig called twice, handler dup — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: logging: basicConfig called twice, handler dup в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-17","logging: JSON formatter not applied","Middle",`<h3>Контекст</h3><p>Python: <b>logging: JSON formatter not applied</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>logging: JSON formatter not applied</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",`error or missing output`,"warn"],["^cat app.py | grep -A2 logging | head -20",`check output`,"warn"],["^sed -i s/basicConfig/dictConfig/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | head -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/basicConfig/dictConfig/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# logging: JSON formatter not applied\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# logging: JSON formatter not applied — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: logging: JSON formatter not applied в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-18","testing: pytest fixture scope function vs module","Senior",`<h3>Контекст</h3><p>Python: <b>testing: pytest fixture scope function vs module</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>testing: pytest fixture scope function vs module</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^pytest -q 2>&1 | tail -20",`error or missing output`,"warn"],["^pytest --collect-only 2>&1 | head -20",`check output`,"warn"],["^pytest --cov=app --cov-report=term 2>&1 | tail -10",`fixed`,"ok"],["^pytest -q 2>&1 | grep -i passed | tail -5",`ok verified`,"ok"]],[{re:"^pytest -q 2>&1 | tail -20",l:"диагностика"},{re:"^pytest --cov=app --cov-report=term 2>&1 | tail -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# testing: pytest fixture scope function vs module\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# testing: pytest fixture scope function vs module — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: testing: pytest fixture scope function vs module в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-19","testing: mock patch where to patch","Junior",`<h3>Контекст</h3><p>Python: <b>testing: mock patch where to patch</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>testing: mock patch where to patch</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^pytest -q 2>&1 | tail -20",`error or missing output`,"warn"],["^pytest --collect-only 2>&1 | head -20",`check output`,"warn"],["^pytest --cov=app --cov-report=term 2>&1 | tail -10",`fixed`,"ok"],["^pytest -q 2>&1 | grep -i passed | tail -5",`ok verified`,"ok"]],[{re:"^pytest -q 2>&1 | tail -20",l:"диагностика"},{re:"^pytest --cov=app --cov-report=term 2>&1 | tail -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# testing: mock patch where to patch\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# testing: mock patch where to patch — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: testing: mock patch where to patch в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-20","asyncio: aiohttp ClientSession leak unclosed","Middle",`<h3>Контекст</h3><p>Python: <b>asyncio: aiohttp ClientSession leak unclosed</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: aiohttp ClientSession leak unclosed</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: aiohttp ClientSession leak unclosed\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: aiohttp ClientSession leak unclosed — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: aiohttp ClientSession leak unclosed в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-21","pathlib: resolve vs absolute","Senior",`<h3>Контекст</h3><p>Python: <b>pathlib: resolve vs absolute</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>pathlib: resolve vs absolute</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",`error or missing output`,"warn"],["^python3 -c \"import pathlib; help(pathlib.Path.joinpath)\" | head -20",`check output`,"warn"],["^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",`fixed`,"ok"],["^python3 app.py && ls out | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",l:"диагностика"},{re:"^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# pathlib: resolve vs absolute\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# pathlib: resolve vs absolute — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: pathlib: resolve vs absolute в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-22","subprocess: Popen communicate deadlock","Junior",`<h3>Контекст</h3><p>Python: <b>subprocess: Popen communicate deadlock</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>subprocess: Popen communicate deadlock</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import subprocess; help(subprocess.run)\" | grep -A2 timeout | head -10",`check output`,"warn"],["^sed -i s/shell=True/shell=False/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/shell=True/shell=False/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# subprocess: Popen communicate deadlock\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# subprocess: Popen communicate deadlock — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: subprocess: Popen communicate deadlock в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-23","socket: TCP_NODELAY not set, latency 40ms","Middle",`<h3>Контекст</h3><p>Python: <b>socket: TCP_NODELAY not set, latency 40ms</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>socket: TCP_NODELAY not set, latency 40ms</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import socket; print(socket.SO_REUSEADDR)\"",`check output`,"warn"],["^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",`fixed`,"ok"],["^python3 server.py & sleep 1; curl -s localhost:8080/healthz | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",l:"диагностика"},{re:"^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# socket: TCP_NODELAY not set, latency 40ms\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# socket: TCP_NODELAY not set, latency 40ms — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: socket: TCP_NODELAY not set, latency 40ms в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-24","requests: timeout tuple connect/read","Senior",`<h3>Контекст</h3><p>Python: <b>requests: timeout tuple connect/read</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>requests: timeout tuple connect/read</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import requests; s=requests.Session(); print(s)\" | head -5",`check output`,"warn"],["^sed -i s/requests.get/requests.Session().get/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i retry | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/requests.get/requests.Session().get/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# requests: timeout tuple connect/read\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# requests: timeout tuple connect/read — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: requests: timeout tuple connect/read в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-25","asyncio: asyncio.Queue maxsize block","Junior",`<h3>Контекст</h3><p>Python: <b>asyncio: asyncio.Queue maxsize block</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: asyncio.Queue maxsize block</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: asyncio.Queue maxsize block\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: asyncio.Queue maxsize block — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: asyncio.Queue maxsize block в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-26","threading: ThreadPoolExecutor vs ProcessPool","Middle",`<h3>Контекст</h3><p>Python: <b>threading: ThreadPoolExecutor vs ProcessPool</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>threading: ThreadPoolExecutor vs ProcessPool</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; print(threading.active_count())\" | head -5",`error or missing output`,"warn"],["^python3 app.py 2>&1 | grep -i race | head -10",`check output`,"warn"],["^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i ok | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; print(threading.active_count())\" | head -5",l:"диагностика"},{re:"^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# threading: ThreadPoolExecutor vs ProcessPool\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# threading: ThreadPoolExecutor vs ProcessPool — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: threading: ThreadPoolExecutor vs ProcessPool в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-27","GIL: multiprocessing 4 cores 3.5x speedup","Senior",`<h3>Контекст</h3><p>Python: <b>GIL: multiprocessing 4 cores 3.5x speedup</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>GIL: multiprocessing 4 cores 3.5x speedup</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; help(threading)\" | grep -i gil | head -5",`error or missing output`,"warn"],["^python3 bench.py 2>&1 | grep -A2 time | head -10",`check output`,"warn"],["^python3 -c \"from multiprocessing import Pool; Pool(4).map(func, range(100))\" 2>&1 | head -10",`fixed`,"ok"],["^python3 bench.py | tail -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; help(threading)\" | grep -i gil | head -5",l:"диагностика"},{re:"^python3 -c \"from multiprocessing import Pool; Pool(4).map(func, range(100))\" 2>&1 | head -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# GIL: multiprocessing 4 cores 3.5x speedup\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# GIL: multiprocessing 4 cores 3.5x speedup — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: GIL: multiprocessing 4 cores 3.5x speedup в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-28","tracemalloc: top 10 snapshot compare","Junior",`<h3>Контекст</h3><p>Python: <b>tracemalloc: top 10 snapshot compare</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>tracemalloc: top 10 snapshot compare</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -X tracemalloc app.py 2>&1 | grep -A5 tracemalloc | head -20",`error or missing output`,"warn"],["^python3 -c \"import tracemalloc; tracemalloc.start(); snap=tracemalloc.take_snapshot(); print(snap.statistics('lineno')[:3])\" | head -10",`check output`,"warn"],["^sed -i s/\"leak\"/\"fixed\"/ app.py",`fixed`,"ok"],["^python3 -X tracemalloc app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -X tracemalloc app.py 2>&1 | grep -A5 tracemalloc | head -20",l:"диагностика"},{re:"^sed -i s/\"leak\"/\"fixed\"/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# tracemalloc: top 10 snapshot compare\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# tracemalloc: top 10 snapshot compare — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: tracemalloc: top 10 snapshot compare в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-29","cProfile: pstats sort cumulative","Middle",`<h3>Контекст</h3><p>Python: <b>cProfile: pstats sort cumulative</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>cProfile: pstats sort cumulative</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -m cProfile -s cumulative app.py 2>&1 | head -30",`error or missing output`,"warn"],["^python3 -m pstats app.prof 2>&1 | head -20",`check output`,"warn"],["^python3 -m cProfile -o app.prof app.py && python3 -c \"import pstats; p=pstats.Stats('app.prof'); p.sort_stats('cumulative').print_stats(5)\" | head -20",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i time | head -5",`ok verified`,"ok"]],[{re:"^python3 -m cProfile -s cumulative app.py 2>&1 | head -30",l:"диагностика"},{re:"^python3 -m cProfile -o app.prof app.py && python3 -c \"import pstats; p=pstats.Stats('app.prof'); p.sort_stats('cumulative').print_stats(5)\" | head -20",l:"исправить"}],{file:"main.py",files:{"main.py":`# cProfile: pstats sort cumulative\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# cProfile: pstats sort cumulative — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: cProfile: pstats sort cumulative в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-30","logging: dictConfig yaml","Senior",`<h3>Контекст</h3><p>Python: <b>logging: dictConfig yaml</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>logging: dictConfig yaml</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",`error or missing output`,"warn"],["^cat app.py | grep -A2 logging | head -20",`check output`,"warn"],["^sed -i s/basicConfig/dictConfig/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | head -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/basicConfig/dictConfig/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# logging: dictConfig yaml\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# logging: dictConfig yaml — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: logging: dictConfig yaml в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-31","testing: parametrize 10 cases","Junior",`<h3>Контекст</h3><p>Python: <b>testing: parametrize 10 cases</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>testing: parametrize 10 cases</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^pytest -q 2>&1 | tail -20",`error or missing output`,"warn"],["^pytest --collect-only 2>&1 | head -20",`check output`,"warn"],["^pytest --cov=app --cov-report=term 2>&1 | tail -10",`fixed`,"ok"],["^pytest -q 2>&1 | grep -i passed | tail -5",`ok verified`,"ok"]],[{re:"^pytest -q 2>&1 | tail -20",l:"диагностика"},{re:"^pytest --cov=app --cov-report=term 2>&1 | tail -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# testing: parametrize 10 cases\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# testing: parametrize 10 cases — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: testing: parametrize 10 cases в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-32","testing: coverage 85% vs 60%","Middle",`<h3>Контекст</h3><p>Python: <b>testing: coverage 85% vs 60%</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>testing: coverage 85% vs 60%</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^pytest -q 2>&1 | tail -20",`error or missing output`,"warn"],["^pytest --collect-only 2>&1 | head -20",`check output`,"warn"],["^pytest --cov=app --cov-report=term 2>&1 | tail -10",`fixed`,"ok"],["^pytest -q 2>&1 | grep -i passed | tail -5",`ok verified`,"ok"]],[{re:"^pytest -q 2>&1 | tail -20",l:"диагностика"},{re:"^pytest --cov=app --cov-report=term 2>&1 | tail -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# testing: coverage 85% vs 60%\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# testing: coverage 85% vs 60% — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: testing: coverage 85% vs 60% в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-33","argparse: subparsers","Senior",`<h3>Контекст</h3><p>Python: <b>argparse: subparsers</b>. Работа с <code>app.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>argparse: subparsers</b>. Файл <code>app.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>app.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>app.py</code>. Активный: <code>app.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat app.py</pre>`,"dev@py:~$",[["^python3 app.py --help | head -20",`error or missing output`,"warn"],["^python3 app.py 2>&1 | grep -i error | head -5",`check output`,"warn"],["^sed -i s/required=False/required=True/ app.py",`fixed`,"ok"],["^python3 app.py --arg val | head -5",`ok verified`,"ok"]],[{re:"^python3 app.py --help | head -20",l:"диагностика"},{re:"^sed -i s/required=False/required=True/ app.py",l:"исправить"}],{file:"app.py",files:{"app.py":`import argparse\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--input\", required=False)  # BUG\n`},checks:[{re:/required\s*=\s*True/,l:"required=True"}],solutionFiles:{"app.py":`import argparse\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--input\", required=True)\n`}} ,{hints:["Симптом: argparse: subparsers в app.py. Ищи причину в этом файле.","Открой app.py в редакторе. Начни с cat app.py.","диагностика → исправить"]});
S("Python","gc-py-34","pathlib: glob **/*.py recursive","Junior",`<h3>Контекст</h3><p>Python: <b>pathlib: glob **/*.py recursive</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>pathlib: glob **/*.py recursive</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",`error or missing output`,"warn"],["^python3 -c \"import pathlib; help(pathlib.Path.joinpath)\" | head -20",`check output`,"warn"],["^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",`fixed`,"ok"],["^python3 app.py && ls out | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",l:"диагностика"},{re:"^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# pathlib: glob **/*.py recursive\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# pathlib: glob **/*.py recursive — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: pathlib: glob **/*.py recursive в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-35","subprocess: check=True vs returncode","Middle",`<h3>Контекст</h3><p>Python: <b>subprocess: check=True vs returncode</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>subprocess: check=True vs returncode</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import subprocess; help(subprocess.run)\" | grep -A2 timeout | head -10",`check output`,"warn"],["^sed -i s/shell=True/shell=False/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/shell=True/shell=False/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# subprocess: check=True vs returncode\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# subprocess: check=True vs returncode — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: subprocess: check=True vs returncode в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-36","socket: select vs poll","Senior",`<h3>Контекст</h3><p>Python: <b>socket: select vs poll</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>socket: select vs poll</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import socket; print(socket.SO_REUSEADDR)\"",`check output`,"warn"],["^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",`fixed`,"ok"],["^python3 server.py & sleep 1; curl -s localhost:8080/healthz | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",l:"диагностика"},{re:"^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# socket: select vs poll\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# socket: select vs poll — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: socket: select vs poll в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-37","requests: backoff exponential","Junior",`<h3>Контекст</h3><p>Python: <b>requests: backoff exponential</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>requests: backoff exponential</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import requests; s=requests.Session(); print(s)\" | head -5",`check output`,"warn"],["^sed -i s/requests.get/requests.Session().get/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i retry | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/requests.get/requests.Session().get/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# requests: backoff exponential\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# requests: backoff exponential — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: requests: backoff exponential в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-38","asyncio: task cancellation CancelledError","Middle",`<h3>Контекст</h3><p>Python: <b>asyncio: task cancellation CancelledError</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: task cancellation CancelledError</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: task cancellation CancelledError\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: task cancellation CancelledError — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: task cancellation CancelledError в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-39","threading: condition wait/notify","Senior",`<h3>Контекст</h3><p>Python: <b>threading: condition wait/notify</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>threading: condition wait/notify</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; print(threading.active_count())\" | head -5",`error or missing output`,"warn"],["^python3 app.py 2>&1 | grep -i race | head -10",`check output`,"warn"],["^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i ok | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; print(threading.active_count())\" | head -5",l:"диагностика"},{re:"^python3 -c \"import threading; lock=threading.Lock(); lock.acquire(); print('locked')\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# threading: condition wait/notify\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# threading: condition wait/notify — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: threading: condition wait/notify в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-40","GIL: C extension releases GIL","Junior",`<h3>Контекст</h3><p>Python: <b>GIL: C extension releases GIL</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>GIL: C extension releases GIL</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import threading; help(threading)\" | grep -i gil | head -5",`error or missing output`,"warn"],["^python3 bench.py 2>&1 | grep -A2 time | head -10",`check output`,"warn"],["^python3 -c \"from multiprocessing import Pool; Pool(4).map(func, range(100))\" 2>&1 | head -10",`fixed`,"ok"],["^python3 bench.py | tail -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import threading; help(threading)\" | grep -i gil | head -5",l:"диагностика"},{re:"^python3 -c \"from multiprocessing import Pool; Pool(4).map(func, range(100))\" 2>&1 | head -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# GIL: C extension releases GIL\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# GIL: C extension releases GIL — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: GIL: C extension releases GIL в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-41","tracemalloc: Traceback 4 frames","Middle",`<h3>Контекст</h3><p>Python: <b>tracemalloc: Traceback 4 frames</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>tracemalloc: Traceback 4 frames</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -X tracemalloc app.py 2>&1 | grep -A5 tracemalloc | head -20",`error or missing output`,"warn"],["^python3 -c \"import tracemalloc; tracemalloc.start(); snap=tracemalloc.take_snapshot(); print(snap.statistics('lineno')[:3])\" | head -10",`check output`,"warn"],["^sed -i s/\"leak\"/\"fixed\"/ app.py",`fixed`,"ok"],["^python3 -X tracemalloc app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -X tracemalloc app.py 2>&1 | grep -A5 tracemalloc | head -20",l:"диагностика"},{re:"^sed -i s/\"leak\"/\"fixed\"/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# tracemalloc: Traceback 4 frames\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# tracemalloc: Traceback 4 frames — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: tracemalloc: Traceback 4 frames в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-42","cProfile: snakeviz","Senior",`<h3>Контекст</h3><p>Python: <b>cProfile: snakeviz</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>cProfile: snakeviz</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -m cProfile -s cumulative app.py 2>&1 | head -30",`error or missing output`,"warn"],["^python3 -m pstats app.prof 2>&1 | head -20",`check output`,"warn"],["^python3 -m cProfile -o app.prof app.py && python3 -c \"import pstats; p=pstats.Stats('app.prof'); p.sort_stats('cumulative').print_stats(5)\" | head -20",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i time | head -5",`ok verified`,"ok"]],[{re:"^python3 -m cProfile -s cumulative app.py 2>&1 | head -30",l:"диагностика"},{re:"^python3 -m cProfile -o app.prof app.py && python3 -c \"import pstats; p=pstats.Stats('app.prof'); p.sort_stats('cumulative').print_stats(5)\" | head -20",l:"исправить"}],{file:"main.py",files:{"main.py":`# cProfile: snakeviz\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# cProfile: snakeviz — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: cProfile: snakeviz в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-43","logging: RotatingFileHandler 10MB","Junior",`<h3>Контекст</h3><p>Python: <b>logging: RotatingFileHandler 10MB</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>logging: RotatingFileHandler 10MB</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",`error or missing output`,"warn"],["^cat app.py | grep -A2 logging | head -20",`check output`,"warn"],["^sed -i s/basicConfig/dictConfig/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | head -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import logging; logging.basicConfig(level='INFO'); logging.info('test')\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/basicConfig/dictConfig/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# logging: RotatingFileHandler 10MB\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# logging: RotatingFileHandler 10MB — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: logging: RotatingFileHandler 10MB в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-44","testing: pytest-xdist -n auto","Middle",`<h3>Контекст</h3><p>Python: <b>testing: pytest-xdist -n auto</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>testing: pytest-xdist -n auto</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^pytest -q 2>&1 | tail -20",`error or missing output`,"warn"],["^pytest --collect-only 2>&1 | head -20",`check output`,"warn"],["^pytest --cov=app --cov-report=term 2>&1 | tail -10",`fixed`,"ok"],["^pytest -q 2>&1 | grep -i passed | tail -5",`ok verified`,"ok"]],[{re:"^pytest -q 2>&1 | tail -20",l:"диагностика"},{re:"^pytest --cov=app --cov-report=term 2>&1 | tail -10",l:"исправить"}],{file:"main.py",files:{"main.py":`# testing: pytest-xdist -n auto\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# testing: pytest-xdist -n auto — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: testing: pytest-xdist -n auto в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-45","asyncio: uvloop 2x speed","Senior",`<h3>Контекст</h3><p>Python: <b>asyncio: uvloop 2x speed</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: uvloop 2x speed</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: uvloop 2x speed\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: uvloop 2x speed — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: uvloop 2x speed в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-46","pathlib: chmod 0o755","Junior",`<h3>Контекст</h3><p>Python: <b>pathlib: chmod 0o755</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>pathlib: chmod 0o755</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",`error or missing output`,"warn"],["^python3 -c \"import pathlib; help(pathlib.Path.joinpath)\" | head -20",`check output`,"warn"],["^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",`fixed`,"ok"],["^python3 app.py && ls out | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"from pathlib import Path; print(Path('a') / 'b')\"",l:"диагностика"},{re:"^python3 -c \"from pathlib import Path; Path('out').mkdir(parents=True, exist_ok=True)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# pathlib: chmod 0o755\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# pathlib: chmod 0o755 — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: pathlib: chmod 0o755 в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-47","subprocess: env PATH","Middle",`<h3>Контекст</h3><p>Python: <b>subprocess: env PATH</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>subprocess: env PATH</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import subprocess; help(subprocess.run)\" | grep -A2 timeout | head -10",`check output`,"warn"],["^sed -i s/shell=True/shell=False/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import subprocess; subprocess.run(['ls', '/tmp'], timeout=5)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/shell=True/shell=False/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# subprocess: env PATH\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# subprocess: env PATH — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: subprocess: env PATH в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-48","socket: getaddrinfo IPv4 vs IPv6","Senior",`<h3>Контекст</h3><p>Python: <b>socket: getaddrinfo IPv4 vs IPv6</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>socket: getaddrinfo IPv4 vs IPv6</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import socket; print(socket.SO_REUSEADDR)\"",`check output`,"warn"],["^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",`fixed`,"ok"],["^python3 server.py & sleep 1; curl -s localhost:8080/healthz | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import socket; s=socket.socket(); s.bind(('127.0.0.1', 8080))\" 2>&1 | head -10",l:"диагностика"},{re:"^python3 -c \"import socket; s=socket.socket(); s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\"",l:"исправить"}],{file:"main.py",files:{"main.py":`# socket: getaddrinfo IPv4 vs IPv6\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# socket: getaddrinfo IPv4 vs IPv6 — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: socket: getaddrinfo IPv4 vs IPv6 в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-49","requests: streaming large 500MB OOM","Junior",`<h3>Контекст</h3><p>Python: <b>requests: streaming large 500MB OOM</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>requests: streaming large 500MB OOM</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",`error or missing output`,"warn"],["^python3 -c \"import requests; s=requests.Session(); print(s)\" | head -5",`check output`,"warn"],["^sed -i s/requests.get/requests.Session().get/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | grep -i retry | head -5",`ok verified`,"ok"]],[{re:"^python3 -c \"import requests; r=requests.get('http://api/healthz', timeout=5); print(r.status_code)\" 2>&1 | head -10",l:"диагностика"},{re:"^sed -i s/requests.get/requests.Session().get/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# requests: streaming large 500MB OOM\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# requests: streaming large 500MB OOM — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: requests: streaming large 500MB OOM в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
S("Python","gc-py-50","asyncio: semaphore 5 concurrency","Middle",`<h3>Контекст</h3><p>Python: <b>asyncio: semaphore 5 concurrency</b>. Работа с <code>main.py</code>.</p><h3>Что происходит</h3><p>Симптом: <b>asyncio: semaphore 5 concurrency</b>. Файл <code>main.py</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>main.py</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>main.py</code>, <code>tests/test_main.py</code>. Активный: <code>main.py</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat main.py</pre>`,"dev@py:~$",[["^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",`error or missing output`,"warn"],["^python3 -c \"import asyncio; help(asyncio.gather)\" | head -20",`check output`,"warn"],["^sed -i s/asyncio.gather/asyncio.wait/ app.py",`fixed`,"ok"],["^python3 app.py 2>&1 | tail -10",`ok verified`,"ok"]],[{re:"^python3 -c \"import asyncio; asyncio.run(main())\" 2>&1 | head -20",l:"диагностика"},{re:"^sed -i s/asyncio.gather/asyncio.wait/ app.py",l:"исправить"}],{file:"main.py",files:{"main.py":`# asyncio: semaphore 5 concurrency\n# broken\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`},checks:[{re:/ok/,l:"fixed"}],solutionFiles:{"main.py":`# asyncio: semaphore 5 concurrency — fixed\n# ok\n`,"tests/test_main.py":`def test_ok():\n    assert True\n`}} ,{hints:["Симптом: asyncio: semaphore 5 concurrency в main.py. Ищи причину в этом файле.","Открой main.py в редакторе. Начни с cat main.py.","диагностика → исправить"]});
