/* Global Playground: Ansible — 25 scenarios */
S("Ansible","gans-1","Ping: UNREACHABLE — неверный порт SSH","Junior", `<h3>Контекст</h3><p>Ansible: <b>Ping: UNREACHABLE — неверный порт SSH</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Ping: UNREACHABLE — неверный порт SSH</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить до и после</li><li>[ ] исправить порт</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible all -m ping -i invento</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить до и после → исправить порт.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible all -m ping -i inventory.ini 2>&1 \\| head -10",`web1 | UNREACHABLE! => Failed to connect: Connection refused`,"err"],
 ["^cat inventory.ini \\| grep web1",`web1 ansible_host=10.0.0.11 ansible_port=22`,"warn"],
 ["^ansible web1 -m ping -e ansible_port=2222 -i inventory.ini",`web1 | SUCCESS`,"ok"],
 ["^sed -i s/ansible_port=22/ansible_port=2222/ inventory.ini",`patched`,"ok"],
 ["^ansible all -m ping -i inventory.ini",`web1 | SUCCESS`,"ok"]
],
[{re:"^ansible all -m ping -i inventory.ini",l:"проверить до и после"},
 {re:"^sed -i s/ansible_port=22/ansible_port=2222/ inventory.ini",l:"исправить порт"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Ping: UNREACHABLE — неверный порт SSH в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить до и после → исправить порт"]});

S("Ansible","gans-2","Idempotency: shell вместо copy — always changed","Middle", `<h3>Контекст</h3><p>Ansible: <b>Idempotency: shell вместо copy — always changed</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Idempotency: shell вместо copy — always changed</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] check mode</li><li>[ ] заменить shell на copy</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml --ch</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: check mode → заменить shell на copy.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml --check --diff 2>&1 \\| grep -A2 CHANGED",`CHANGED: shell: echo config > /etc/app.conf`,"warn"],
 ["^cat roles/app/tasks/main.yml \\| grep -A2 shell",`shell: echo config > /etc/app.conf`,"err"],
 ["^sed -i s/shell:\\ echo/copy:\\\\n\\ \\ \\ \\ content:\\ config/ roles/app/tasks/main.yml",`switched to copy`,"ok"],
 ["^ansible-playbook site.yml --check 2>&1 \\| tail -5",`ok=3 changed=0`,"ok"]
],
[{re:"^ansible-playbook site.yml --check --diff",l:"check mode"},
 {re:"copy:",l:"заменить shell на copy"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Idempotency: shell вместо copy — always changed в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: check mode → заменить shell на copy"]});

S("Ansible","gans-3","Vault: нужен пароль — ERROR vault password is required","Middle", `<h3>Контекст</h3><p>Ansible: <b>Vault: нужен пароль — ERROR vault password is required</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Vault: нужен пароль — ERROR vault password is required</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] запуск с паролем</li><li>[ ] файл с паролем</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml 2>&1</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: запуск с паролем → файл с паролем.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml 2>&1 \\| grep -i vault",`ERROR: vault password is required`,"err"],
 ["^ansible-playbook site.yml --ask-vault-pass 2>&1 \\| tail -5",`Vault password: \nPLAY RECAP ok=5`,"ok"],
 ["^ansible-playbook site.yml --vault-password-file ~/.vault_pass 2>&1 \\| tail -3",`ok=5`,"ok"]
],
[{re:"--ask-vault-pass",l:"запуск с паролем"},
 {re:"--vault-password-file",l:"файл с паролем"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Vault: нужен пароль — ERROR vault password is required в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: запуск с паролем → файл с паролем"]});

S("Ansible","gans-4","Gather facts тормозит — отключить для скорости","Middle", `<h3>Контекст</h3><p>Ansible: <b>Gather facts тормозит — отключить для скорости</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Gather facts тормозит — отключить для скорости</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] замерить время</li><li>[ ] отключить сбор фактов</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml 2>&1</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: замерить время → отключить сбор фактов.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"Gathering Facts\"",`ok: \\[web1\\]`,"warn"],
 ["^cat site.yml \\| grep gather_facts",`(пусто) — дефолт true`,"warn"],
 ["^sed -i s/hosts:\\ all/hosts:\\ all\\n\\ \\ gather_facts:\\ false/ site.yml",`patched`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -c \"Gathering Facts\"",`0`,"ok"]
],
[{re:"^ansible-playbook site.yml",l:"замерить время"},
 {re:"gather_facts: false",l:"отключить сбор фактов"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Gather facts тормозит — отключить для скорости в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: замерить время → отключить сбор фактов"]});

S("Ansible","gans-5","Become: permission denied без sudo","Junior", `<h3>Контекст</h3><p>Ansible: <b>Become: permission denied без sudo</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Become: permission denied без sudo</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] найти ошибку</li><li>[ ] включить become</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml 2>&1</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: найти ошибку → включить become.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"permission denied\"",`permission denied /etc/nginx/nginx.conf`,"err"],
 ["^cat site.yml \\| grep become",`(пусто)`,"err"],
 ["^sed -i s/hosts:\\ all/hosts:\\ all\\n\\ \\ become:\\ true/ site.yml",`patched become`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| tail -5",`ok=4`,"ok"]
],
[{re:"permission denied",l:"найти ошибку"},
 {re:"become: true",l:"включить become"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Become: permission denied без sudo в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: найти ошибку → включить become"]});

S("Ansible","gans-6","Handler не срабатывает — notify опечатка","Middle", `<h3>Контекст</h3><p>Ansible: <b>Handler не срабатывает — notify опечатка</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Handler не срабатывает — notify опечатка</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] найти опечатку</li><li>[ ] исправить имя handler</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat roles/nginx/handlers/main.</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: найти опечатку → исправить имя handler.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat roles/nginx/handlers/main.yml \\| grep -A2 name",`name: restart nginx`,"ok"],
 ["^cat roles/nginx/tasks/main.yml \\| grep notify",`notify: restar nginx`,"err"],
 ["^sed -i s/restar\\ nginx/restart\\ nginx/ roles/nginx/tasks/main.yml",`fixed`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 handler",`RUNNING HANDLER restart nginx`,"ok"]
],
[{re:"^cat roles/nginx/tasks/main.yml.*notify",l:"найти опечатку"},
 {re:"^sed -i",l:"исправить имя handler"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Handler не срабатывает — notify опечатка в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: найти опечатку → исправить имя handler"]});

S("Ansible","gans-7","Template: переменная undefined — AnsibleUndefinedVariable","Middle", `<h3>Контекст</h3><p>Ansible: <b>Template: переменная undefined — AnsibleUndefinedVariable</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Template: переменная undefined — AnsibleUndefinedVariable</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] найти отсутствующую переменную</li><li>[ ] добавить</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml 2>&1</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: найти отсутствующую переменную → добавить.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"undefined\"",`'app_port' is undefined`,"err"],
 ["^cat group_vars/all.yml \\| grep app_port",`(пусто)`,"err"],
 ["^echo \"app_port: 8080\" >> group_vars/all.yml",`added`,"ok"],
 ["^ansible-playbook site.yml --check 2>&1 \\| tail -3",`ok=3`,"ok"]
],
[{re:"'app_port' is undefined",l:"найти отсутствующую переменную"},
 {re:"^echo \"app_port: 8080\" >> group_vars/all.yml",l:"добавить"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Template: переменная undefined — AnsibleUndefinedVariable в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: найти отсутствующую переменную → добавить"]});

S("Ansible","gans-8","Galaxy: роль не установлена — could not find role","Junior", `<h3>Контекст</h3><p>Ansible: <b>Galaxy: роль не установлена — could not find role</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Galaxy: роль не установлена — could not find role</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] ошибка роли</li><li>[ ] установить</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml 2>&1</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: ошибка роли → установить.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"could not find role\"",`could not find role geerlingguy.nginx`,"err"],
 ["^cat requirements.yml \\| grep geerlingguy",`src: geerlingguy.nginx`,"ok"],
 ["^ansible-galaxy install -r requirements.yml",`Installing geerlingguy.nginx`,"ok"],
 ["^ansible-playbook site.yml --check 2>&1 \\| tail -3",`ok=3`,"ok"]
],
[{re:"could not find role",l:"ошибка роли"},
 {re:"^ansible-galaxy install -r requirements.yml",l:"установить"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Galaxy: роль не установлена — could not find role в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: ошибка роли → установить"]});

S("Ansible","gans-9","Molecule: test падает на idempotence","Senior", `<h3>Контекст</h3><p>Ansible: <b>Molecule: test падает на idempotence</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Molecule: test падает на idempotence</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] прогнать</li><li>[ ] проверить</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>molecule test 2>&1 \\\\| grep -A</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: прогнать → проверить.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^molecule test 2>&1 \\| grep -A3 idempotence",`Idempotence completed: failures=1`,"err"],
 ["^molecule converge 2>&1 \\| grep -A2 CHANGED",`CHANGED shell`,"warn"],
 ["^sed -i s/shell:/copy:/ roles/app/tasks/main.yml",`fixed`,"ok"],
 ["^molecule test 2>&1 \\| tail -5",`Idempotence completed: failures=0`,"ok"]
],
[{re:"^molecule test",l:"прогнать"},
 {re:"Idempotence completed",l:"проверить"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Molecule: test падает на idempotence в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: прогнать → проверить"]});

S("Ansible","gans-10","Inventory: хост в двух группах — переменные конфликтуют","Middle", `<h3>Контекст</h3><p>Ansible: <b>Inventory: хост в двух группах — переменные конфликтуют</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Inventory: хост в двух группах — переменные конфликтуют</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить переменные хоста</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat inventory.ini</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить переменные хоста.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat inventory.ini",`\\[prod\\]\nweb1\n\\[staging\\]\nweb1`,"warn"],
 ["^ansible-inventory -i inventory.ini --host web1 2>&1 \\| grep -A2 env",`env: prod`,"warn"],
 ["^cat inventory.ini \\| grep -n web1",`2:web1\n5:web1`,"err"],
 ["^sed -i /\\[staging\\]/a\\web2/ inventory.ini",`separated`,"ok"]
],
[{re:"^ansible-inventory -i inventory.ini --host web1",l:"проверить переменные хоста"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Inventory: хост в двух группах — переменные конфликтуют в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить переменные хоста"]});

S("Ansible","gans-11","Serial: rolling update по 1 хосту","Middle", `<h3>Контекст</h3><p>Ansible: <b>Serial: rolling update по 1 хосту</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Serial: rolling update по 1 хосту</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить стратегию</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat site.yml \\\\| grep serial</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить стратегию.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat site.yml \\| grep serial",`serial: 1`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"PLAY.*web1\"",`PLAY \\[web1\\]`,"ok"]
],
[{re:"serial: 1",l:"проверить стратегию"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Serial: rolling update по 1 хосту в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить стратегию"]});

S("Ansible","gans-12","Check mode: --diff показать что изменится","Junior", `<h3>Контекст</h3><p>Ansible: <b>Check mode: --diff показать что изменится</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Check mode: --diff показать что изменится</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] check diff</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-playbook site.yml -C -</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: check diff.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-playbook site.yml -C -D 2>&1 \\| head -30",`--- before\n+++ after\n@@ -1 +1 @@\n-listen 80`,"ok"]
],
[{re:"^ansible-playbook site.yml -C -D",l:"check diff"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Check mode: --diff показать что изменится в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: check diff"]});

S("Ansible","gans-13","Tags: запустить только deploy","Junior", `<h3>Контекст</h3><p>Ansible: <b>Tags: запустить только deploy</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Tags: запустить только deploy</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] запуск по тегу</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat site.yml \\\\| grep tags</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: запуск по тегу.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat site.yml \\| grep tags",`tags: deploy`,"ok"],
 ["^ansible-playbook site.yml --tags deploy 2>&1 \\| tail -10",`ok=2`,"ok"],
 ["^ansible-playbook site.yml --skip-tags deploy 2>&1 \\| tail -10",`ok=2 (без deploy)`,"ok"]
],
[{re:"^ansible-playbook site.yml --tags deploy",l:"запуск по тегу"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Tags: запустить только deploy в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: запуск по тегу"]});

S("Ansible","gans-14","Delegate_to: задача должна бежать на localhost, а не на remote","Middle", `<h3>Контекст</h3><p>Ansible: <b>Delegate_to: задача должна бежать на localhost, а не на remote</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Delegate_to: задача должна бежать на localhost, а не на remote</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] делегировать на локалхост</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat roles/app/tasks/main.yml \\</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: делегировать на локалхост.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat roles/app/tasks/main.yml \\| grep -A3 uri:",`uri:\n  url: http://api.corp.local/healthz`,"warn"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"uri.*failed\"",`Connection refused`,"err"],
 ["^sed -i s/url:\\ http:\\/\\/api.corp.local\\/healthz/url:\\ http:\\/\\/api.corp.local\\/healthz\\n\\ \\ \\ \\ delegate_to:\\ localhost/ roles/app/tasks/main.yml",`added delegate_to`,"ok"]
],
[{re:"delegate_to: localhost",l:"делегировать на локалхост"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Delegate_to: задача должна бежать на localhost, а не на remote в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: делегировать на локалхост"]});

S("Ansible","gans-15","Loop: создать 3 пользователей через loop","Junior", `<h3>Контекст</h3><p>Ansible: <b>Loop: создать 3 пользователей через loop</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Loop: создать 3 пользователей через loop</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить цикл</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat roles/users/tasks/main.yml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить цикл.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat roles/users/tasks/main.yml \\| grep -A4 loop",`loop: [alice, bob, carol]`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 \"user.*alice\"",`changed: \\[web1\\] (alice)`,"ok"]
],
[{re:"loop:",l:"проверить цикл"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Loop: создать 3 пользователей через loop в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить цикл"]});

S("Ansible","gans-16","Failed_when: команда вернула rc 0 но логически failed","Middle", `<h3>Контекст</h3><p>Ansible: <b>Failed_when: команда вернула rc 0 но логически failed</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Failed_when: команда вернула rc 0 но логически failed</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] кастомное условие провала</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat roles/app/tasks/main.yml \\</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: кастомное условие провала.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat roles/app/tasks/main.yml \\| grep -A3 failed_when",`failed_when: \"'ERROR' in result.stdout\"`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 FAILED",`FAILED`,"ok"]
],
[{re:"failed_when",l:"кастомное условие провала"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Failed_when: команда вернула rc 0 но логически failed в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: кастомное условие провала"]});

S("Ansible","gans-17","Block/rescue: откат при падении миграции","Senior", `<h3>Контекст</h3><p>Ansible: <b>Block/rescue: откат при падении миграции</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Block/rescue: откат при падении миграции</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] найти блок</li><li>[ ] проверить rescue</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat site.yml \\\\| grep -A5 bloc</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: найти блок → проверить rescue.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat site.yml \\| grep -A5 block:",`block:\n  - name: migrate\n  rescue:\n    - name: rollback`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep -A2 rescue",`rescue`,"ok"]
],
[{re:"block:",l:"найти блок"},
 {re:"rescue:",l:"проверить rescue"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Block/rescue: откат при падении миграции в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: найти блок → проверить rescue"]});

S("Ansible","gans-18","Fact caching: ускорить повторные прогоны","Middle", `<h3>Контекст</h3><p>Ansible: <b>Fact caching: ускорить повторные прогоны</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Fact caching: ускорить повторные прогоны</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить кеш</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat ansible.cfg \\\\| grep -A2 f</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить кеш.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat ansible.cfg \\| grep -A2 fact_caching",`fact_caching = jsonfile\nfact_caching_connection = /tmp/facts`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep \"Gathering Facts\" -A1",`(кеш) ok`,"ok"]
],
[{re:"fact_caching",l:"проверить кеш"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Fact caching: ускорить повторные прогоны в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить кеш"]});

S("Ansible","gans-19","Strategy free vs linear — параллельность","Senior", `<h3>Контекст</h3><p>Ansible: <b>Strategy free vs linear — параллельность</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Strategy free vs linear — параллельность</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] стратегия free</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat site.yml \\\\| grep strategy</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: стратегия free.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat site.yml \\| grep strategy",`strategy: free`,"ok"],
 ["^ansible-playbook site.yml 2>&1 \\| grep PLAY -A1",`free: parallel`,"ok"]
],
[{re:"strategy: free",l:"стратегия free"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Strategy free vs linear — параллельность в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: стратегия free"]});

S("Ansible","gans-20","Ansible-lint: yaml без отступов","Junior", `<h3>Контекст</h3><p>Ansible: <b>Ansible-lint: yaml без отступов</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Ansible-lint: yaml без отступов</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] линт</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible-lint site.yml 2>&1 \\\\|</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: линт.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible-lint site.yml 2>&1 \\| head -20",`yaml\\[truthy\\]: truthy value should be one of`,"warn"],
 ["^ansible-lint --fix site.yml",`Fixed`,"ok"]
],
[{re:"^ansible-lint site.yml",l:"линт"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Ansible-lint: yaml без отступов в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: линт"]});

S("Ansible","gans-21","AWX/Tower: job template не видит inventory","Middle", `<h3>Контекст</h3><p>Ansible: <b>AWX/Tower: job template не видит inventory</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>AWX/Tower: job template не видит inventory</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить шаблон</li><li>[ ] привязать inventory</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>awx job_templates list 2>&1 \\\\</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить шаблон → привязать inventory.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^awx job_templates list 2>&1 \\| grep -A2 inventory",`inventory: null`,"err"],
 ["^awx job_templates modify 1 --inventory prod",`modified`,"ok"]
],
[{re:"^awx job_templates list",l:"проверить шаблон"},
 {re:"^awx job_templates modify 1 --inventory prod",l:"привязать inventory"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: AWX/Tower: job template не видит inventory в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить шаблон → привязать inventory"]});

S("Ansible","gans-22","Windows: win_ping падает — нужен WinRM","Middle", `<h3>Контекст</h3><p>Ansible: <b>Windows: win_ping падает — нужен WinRM</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Windows: win_ping падает — нужен WinRM</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверка Windows</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ansible win -m win_ping -i inv</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверка Windows.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ansible win -m win_ping -i inventory.ini 2>&1 \\| head -10",`UNREACHABLE WinRM`,"err"],
 ["^cat inventory.ini \\| grep win",`win ansible_connection=winrm`,"ok"]
],
[{re:"^ansible win -m win_ping",l:"проверка Windows"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Windows: win_ping падает — нужен WinRM в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверка Windows"]});

S("Ansible","gans-23","Collections: community.general vs ansible.builtin","Middle", `<h3>Контекст</h3><p>Ansible: <b>Collections: community.general vs ansible.builtin</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Collections: community.general vs ansible.builtin</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] проверить коллекцию</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat roles/app/tasks/main.yml \\</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: проверить коллекцию.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat roles/app/tasks/main.yml \\| grep -c community\\.general",`3`,"ok"],
 ["^ansible-galaxy collection install community.general",`Installed`,"ok"]
],
[{re:"community\\.general",l:"проверить коллекцию"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Collections: community.general vs ansible.builtin в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: проверить коллекцию"]});

S("Ansible","gans-24","Pulled inventory: динамический inventory из AWS EC2","Senior", `<h3>Контекст</h3><p>Ansible: <b>Pulled inventory: динамический inventory из AWS EC2</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Pulled inventory: динамический inventory из AWS EC2</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] граф динамического inventory</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>cat inventory.aws_ec2.yml \\\\| </code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: граф динамического inventory.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^cat inventory.aws_ec2.yml \\| head -10",`plugin: aws_ec2\nregions: [eu-central-1]`,"ok"],
 ["^ansible-inventory -i inventory.aws_ec2.yml --graph 2>&1 \\| head -20",`@all:\n  |--@aws_ec2:`,"ok"]
],
[{re:"^ansible-inventory -i inventory.aws_ec2.yml --graph",l:"граф динамического inventory"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Pulled inventory: динамический inventory из AWS EC2 в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: граф динамического inventory"]});

S("Ansible","gans-25","Callback json: разобрать вывод в CI","Junior", `<h3>Контекст</h3><p>Ansible: <b>Callback json: разобрать вывод в CI</b>. Работа с <code>ansible/site.yml</code> в проекте.</p><h3>Что происходит</h3><p>Симптом: <b>Callback json: разобрать вывод в CI</b>. Файл <code>ansible/site.yml</code> содержит ошибку, проверки падают.</p><h3>Что нужно сделать</h3><ul><li>[ ] json вывод</li></ul><h3>Ограничения</h3><p>Меняйте только файлы проекта (активный: <code>ansible/site.yml</code>).</p><h3>Стартовое состояние</h3><p>Файлы: <code>ansible/site.yml</code>, <code>ansible/inventory.ini</code>. Активный файл открыт в редакторе. Начните с <code>ANSIBLE_STDOUT_CALLBACK=json a</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный: json вывод.</p><h3>Проверка</h3><pre>cat ansible/site.yml<br>проверить код</pre>`,
"dev@lab:~$",
[
 ["^ANSIBLE_STDOUT_CALLBACK=json ansible-playbook site.yml 2>&1 \\| jq .stats -c",`{\"web1\":{\"ok\":4}}`,"ok"]
],
[{re:"ANSIBLE_STDOUT_CALLBACK=json",l:"json вывод"}],{file:"ansible/site.yml",files:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - command: echo broken\n`,"ansible/inventory.ini":`[web]\nweb1\n`},checks:[{re:/ansible\.builtin/,l:"ansible module"}],solutionFiles:{"ansible/site.yml":`- hosts: web\n  tasks:\n    - ansible.builtin.command:\n        cmd: echo fixed\n`,"ansible/inventory.ini":`[web]\nweb1\n`}},{hints:["Симптом: Callback json: разобрать вывод в CI в ansible/site.yml. Ищи причину в коде/конфиге этого файла.","Открой ansible/site.yml в редакторе, проверь логику. Инструменты: cat, ls, grep. Начни с cat ansible/site.yml.","Порядок: json вывод"]});
