/* Global Playground: scenarios-global-dr-sre.js — enriched with filesystem */
S("SRE DR","gc-dr-1","Velero backup: schedule missed 2 days","Junior",`<h3>Контекст</h3><p>SRE DR: <b>Velero backup: schedule missed 2 days</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero backup: schedule missed 2 days</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero backup get | grep -A2 prod-daily",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero backup create prod-manual --include-namespaces prod --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero backup get | grep -A2 prod-daily",l:"диагностика"},{re:"^velero backup create prod-manual --include-namespaces prod --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero backup: schedule missed 2 days в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-2","Velero restore: PVC not bound after restore","Middle",`<h3>Контекст</h3><p>SRE DR: <b>Velero restore: PVC not bound after restore</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero restore: PVC not bound after restore</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero restore get | grep -A2 prod-restore",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero restore create --from-backup prod-daily --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero restore get | grep -A2 prod-restore",l:"диагностика"},{re:"^velero restore create --from-backup prod-daily --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero restore: PVC not bound after restore в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-3","etcd snapshot save: 2.1GB DB size NOSPACE","Senior",`<h3>Контекст</h3><p>SRE DR: <b>etcd snapshot save: 2.1GB DB size NOSPACE</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd snapshot save: 2.1GB DB size NOSPACE</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db | tail -3",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db | tail -3",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd snapshot save: 2.1GB DB size NOSPACE в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-4","etcd snapshot restore: data-dir mismatch","Junior",`<h3>Контекст</h3><p>SRE DR: <b>etcd snapshot restore: data-dir mismatch</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd snapshot restore: data-dir mismatch</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db | tail -3",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db | tail -3",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd snapshot restore: data-dir mismatch в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-5","RPO 15m vs RTO 1h — не укладываемся","Middle",`<h3>Контекст</h3><p>SRE DR: <b>RPO 15m vs RTO 1h — не укладываемся</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>RPO 15m vs RTO 1h — не укладываемся</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat docs/runbook/dr.md | grep -A2 RPO",`RPO 15m RTO 1h`,"warn"],["^velero backup get -o json | jq -r '.items[0].status.expiration'",`expiration 2026-08-25`,"warn"],["^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",`updated`,"ok"],["^cat docs/runbook/dr.md | grep RTO",`RTO 1h`,"ok"]],[{re:"^cat docs/runbook/dr.md | grep -A2 RPO",l:"диагностика"},{re:"^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: RPO 15m vs RTO 1h — не укладываемся в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-6","SLO: burn rate 14x, error budget 2%","Senior",`<h3>Контекст</h3><p>SRE DR: <b>SLO: burn rate 14x, error budget 2%</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>SLO: burn rate 14x, error budget 2%</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat slo.yaml | grep -A2 burnRate",`burn rate 14x`,"warn"],["^promtool query instant http://prometheus:9090 'burn_rate'",`14`,"warn"],["^kubectl apply -f slo-fix.yaml",`applied`,"ok"],["^cat slo.yaml | grep -A2 target",`target 99.9`,"ok"]],[{re:"^cat slo.yaml | grep -A2 burnRate",l:"диагностика"},{re:"^kubectl apply -f slo-fix.yaml",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: SLO: burn rate 14x, error budget 2% в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-7","SLO: latency p99 800ms > 200ms","Junior",`<h3>Контекст</h3><p>SRE DR: <b>SLO: latency p99 800ms > 200ms</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>SLO: latency p99 800ms > 200ms</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat slo.yaml | grep -A2 burnRate",`burn rate 14x`,"warn"],["^promtool query instant http://prometheus:9090 'burn_rate'",`14`,"warn"],["^kubectl apply -f slo-fix.yaml",`applied`,"ok"],["^cat slo.yaml | grep -A2 target",`target 99.9`,"ok"]],[{re:"^cat slo.yaml | grep -A2 burnRate",l:"диагностика"},{re:"^kubectl apply -f slo-fix.yaml",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: SLO: latency p99 800ms > 200ms в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-8","Error budget: релиз заблокирован, 0%","Middle",`<h3>Контекст</h3><p>SRE DR: <b>Error budget: релиз заблокирован, 0%</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Error budget: релиз заблокирован, 0%</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat error-budget.json | jq .remaining",`remaining 2%`,"warn"],["^cat slo.yaml | grep -A3 errorBudget",`errorBudget 100%`,"warn"],["^echo \"freeze release\" >> error-budget.json",`freeze`,"ok"],["^cat error-budget.json | jq .policy",`policy 50% halt`,"ok"]],[{re:"^cat error-budget.json | jq .remaining",l:"диагностика"},{re:"^echo \"freeze release\" >> error-budget.json",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Error budget: релиз заблокирован, 0% в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-9","Postmortem: incident 2h, нет action items","Senior",`<h3>Контекст</h3><p>SRE DR: <b>Postmortem: incident 2h, нет action items</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Postmortem: incident 2h, нет action items</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat postmortem/2026-08-24.md | head -20",`# Incident 2026-08-24 2h`,"warn"],["^cat postmortem/2026-08-24.md | grep -i \"Action Items\"",`no action items`,"warn"],["^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",`added`,"ok"],["^cat postmortem/2026-08-24.md | grep -c \"Action\"",`3`,"ok"]],[{re:"^cat postmortem/2026-08-24.md | head -20",l:"диагностика"},{re:"^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Postmortem: incident 2h, нет action items в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-10","Velero: restic vs kopia uploader","Junior",`<h3>Контекст</h3><p>SRE DR: <b>Velero: restic vs kopia uploader</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero: restic vs kopia uploader</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero backup get | grep -A2 prod-daily",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero backup create prod-manual --include-namespaces prod --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero backup get | grep -A2 prod-daily",l:"диагностика"},{re:"^velero backup create prod-manual --include-namespaces prod --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero: restic vs kopia uploader в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-11","etcd: compact revision 12345 failed","Middle",`<h3>Контекст</h3><p>SRE DR: <b>etcd: compact revision 12345 failed</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd: compact revision 12345 failed</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd: compact revision 12345 failed в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-12","etcd: defrag needed, 50% fragmentation","Senior",`<h3>Контекст</h3><p>SRE DR: <b>etcd: defrag needed, 50% fragmentation</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd: defrag needed, 50% fragmentation</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd: defrag needed, 50% fragmentation в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-13","RPO: cross-region replication lag 30m","Junior",`<h3>Контекст</h3><p>SRE DR: <b>RPO: cross-region replication lag 30m</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>RPO: cross-region replication lag 30m</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat docs/runbook/dr.md | grep -A2 RPO",`RPO 15m RTO 1h`,"warn"],["^velero backup get -o json | jq -r '.items[0].status.expiration'",`expiration 2026-08-25`,"warn"],["^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",`updated`,"ok"],["^cat docs/runbook/dr.md | grep RTO",`RTO 1h`,"ok"]],[{re:"^cat docs/runbook/dr.md | grep -A2 RPO",l:"диагностика"},{re:"^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: RPO: cross-region replication lag 30m в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-14","SLO: SLI availability 99.9% vs 99.95%","Middle",`<h3>Контекст</h3><p>SRE DR: <b>SLO: SLI availability 99.9% vs 99.95%</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>SLO: SLI availability 99.9% vs 99.95%</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat slo.yaml | grep -A2 burnRate",`burn rate 14x`,"warn"],["^promtool query instant http://prometheus:9090 'burn_rate'",`14`,"warn"],["^kubectl apply -f slo-fix.yaml",`applied`,"ok"],["^cat slo.yaml | grep -A2 target",`target 99.9`,"ok"]],[{re:"^cat slo.yaml | grep -A2 burnRate",l:"диагностика"},{re:"^kubectl apply -f slo-fix.yaml",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: SLO: SLI availability 99.9% vs 99.95% в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-15","Error budget: burndown chart 30d","Senior",`<h3>Контекст</h3><p>SRE DR: <b>Error budget: burndown chart 30d</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Error budget: burndown chart 30d</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat error-budget.json | jq .remaining",`remaining 2%`,"warn"],["^cat slo.yaml | grep -A3 errorBudget",`errorBudget 100%`,"warn"],["^echo \"freeze release\" >> error-budget.json",`freeze`,"ok"],["^cat error-budget.json | jq .policy",`policy 50% halt`,"ok"]],[{re:"^cat error-budget.json | jq .remaining",l:"диагностика"},{re:"^echo \"freeze release\" >> error-budget.json",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Error budget: burndown chart 30d в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-16","Postmortem: blameless template","Junior",`<h3>Контекст</h3><p>SRE DR: <b>Postmortem: blameless template</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Postmortem: blameless template</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat postmortem/2026-08-24.md | head -20",`# Incident 2026-08-24 2h`,"warn"],["^cat postmortem/2026-08-24.md | grep -i \"Action Items\"",`no action items`,"warn"],["^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",`added`,"ok"],["^cat postmortem/2026-08-24.md | grep -c \"Action\"",`3`,"ok"]],[{re:"^cat postmortem/2026-08-24.md | head -20",l:"диагностика"},{re:"^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Postmortem: blameless template в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-17","Velero: BSL bucket not reachable 403","Middle",`<h3>Контекст</h3><p>SRE DR: <b>Velero: BSL bucket not reachable 403</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero: BSL bucket not reachable 403</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero backup get | grep -A2 prod-daily",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero backup create prod-manual --include-namespaces prod --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero backup get | grep -A2 prod-daily",l:"диагностика"},{re:"^velero backup create prod-manual --include-namespaces prod --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero: BSL bucket not reachable 403 в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-18","etcd: member list 1 unhealthy","Senior",`<h3>Контекст</h3><p>SRE DR: <b>etcd: member list 1 unhealthy</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd: member list 1 unhealthy</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd: member list 1 unhealthy в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-19","RTO: restore 45m vs 1h","Junior",`<h3>Контекст</h3><p>SRE DR: <b>RTO: restore 45m vs 1h</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>RTO: restore 45m vs 1h</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat docs/runbook/dr.md | grep -A2 RPO",`RPO 15m RTO 1h`,"warn"],["^velero backup get -o json | jq -r '.items[0].status.expiration'",`expiration 2026-08-25`,"warn"],["^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",`updated`,"ok"],["^cat docs/runbook/dr.md | grep RTO",`RTO 1h`,"ok"]],[{re:"^cat docs/runbook/dr.md | grep -A2 RPO",l:"диагностика"},{re:"^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: RTO: restore 45m vs 1h в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-20","SLO: multi-window burn alert","Middle",`<h3>Контекст</h3><p>SRE DR: <b>SLO: multi-window burn alert</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>SLO: multi-window burn alert</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat slo.yaml | grep -A2 burnRate",`burn rate 14x`,"warn"],["^promtool query instant http://prometheus:9090 'burn_rate'",`14`,"warn"],["^kubectl apply -f slo-fix.yaml",`applied`,"ok"],["^cat slo.yaml | grep -A2 target",`target 99.9`,"ok"]],[{re:"^cat slo.yaml | grep -A2 burnRate",l:"диагностика"},{re:"^kubectl apply -f slo-fix.yaml",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: SLO: multi-window burn alert в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-21","Error budget: feature freeze","Senior",`<h3>Контекст</h3><p>SRE DR: <b>Error budget: feature freeze</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Error budget: feature freeze</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat error-budget.json | jq .remaining",`remaining 2%`,"warn"],["^cat slo.yaml | grep -A3 errorBudget",`errorBudget 100%`,"warn"],["^echo \"freeze release\" >> error-budget.json",`freeze`,"ok"],["^cat error-budget.json | jq .policy",`policy 50% halt`,"ok"]],[{re:"^cat error-budget.json | jq .remaining",l:"диагностика"},{re:"^echo \"freeze release\" >> error-budget.json",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Error budget: feature freeze в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-22","Postmortem: 5 whys","Junior",`<h3>Контекст</h3><p>SRE DR: <b>Postmortem: 5 whys</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Postmortem: 5 whys</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat postmortem/2026-08-24.md | head -20",`# Incident 2026-08-24 2h`,"warn"],["^cat postmortem/2026-08-24.md | grep -i \"Action Items\"",`no action items`,"warn"],["^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",`added`,"ok"],["^cat postmortem/2026-08-24.md | grep -c \"Action\"",`3`,"ok"]],[{re:"^cat postmortem/2026-08-24.md | head -20",l:"диагностика"},{re:"^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Postmortem: 5 whys в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-23","Velero: backup TTL 30d vs 7d","Middle",`<h3>Контекст</h3><p>SRE DR: <b>Velero: backup TTL 30d vs 7d</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero: backup TTL 30d vs 7d</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero backup get | grep -A2 prod-daily",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero backup create prod-manual --include-namespaces prod --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero backup get | grep -A2 prod-daily",l:"диагностика"},{re:"^velero backup create prod-manual --include-namespaces prod --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero: backup TTL 30d vs 7d в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-24","etcd: snapshot status hash mismatch","Senior",`<h3>Контекст</h3><p>SRE DR: <b>etcd: snapshot status hash mismatch</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd: snapshot status hash mismatch</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db | tail -3",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db | tail -3",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl snapshot save /tmp/etcd.db",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd: snapshot status hash mismatch в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-25","RPO: async vs sync replication","Junior",`<h3>Контекст</h3><p>SRE DR: <b>RPO: async vs sync replication</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>RPO: async vs sync replication</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat docs/runbook/dr.md | grep -A2 RPO",`RPO 15m RTO 1h`,"warn"],["^velero backup get -o json | jq -r '.items[0].status.expiration'",`expiration 2026-08-25`,"warn"],["^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",`updated`,"ok"],["^cat docs/runbook/dr.md | grep RTO",`RTO 1h`,"ok"]],[{re:"^cat docs/runbook/dr.md | grep -A2 RPO",l:"диагностика"},{re:"^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: RPO: async vs sync replication в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-26","SLO: apdex 0.85","Middle",`<h3>Контекст</h3><p>SRE DR: <b>SLO: apdex 0.85</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>SLO: apdex 0.85</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat slo.yaml | grep -A2 burnRate",`burn rate 14x`,"warn"],["^promtool query instant http://prometheus:9090 'burn_rate'",`14`,"warn"],["^kubectl apply -f slo-fix.yaml",`applied`,"ok"],["^cat slo.yaml | grep -A2 target",`target 99.9`,"ok"]],[{re:"^cat slo.yaml | grep -A2 burnRate",l:"диагностика"},{re:"^kubectl apply -f slo-fix.yaml",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: SLO: apdex 0.85 в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-27","Error budget: 28d rolling window","Senior",`<h3>Контекст</h3><p>SRE DR: <b>Error budget: 28d rolling window</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Error budget: 28d rolling window</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat error-budget.json | jq .remaining",`remaining 2%`,"warn"],["^cat slo.yaml | grep -A3 errorBudget",`errorBudget 100%`,"warn"],["^echo \"freeze release\" >> error-budget.json",`freeze`,"ok"],["^cat error-budget.json | jq .policy",`policy 50% halt`,"ok"]],[{re:"^cat error-budget.json | jq .remaining",l:"диагностика"},{re:"^echo \"freeze release\" >> error-budget.json",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Error budget: 28d rolling window в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-28","Postmortem: incident severity SEV1","Junior",`<h3>Контекст</h3><p>SRE DR: <b>Postmortem: incident severity SEV1</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Postmortem: incident severity SEV1</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat postmortem/2026-08-24.md | head -20",`# Incident 2026-08-24 2h`,"warn"],["^cat postmortem/2026-08-24.md | grep -i \"Action Items\"",`no action items`,"warn"],["^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",`added`,"ok"],["^cat postmortem/2026-08-24.md | grep -c \"Action\"",`3`,"ok"]],[{re:"^cat postmortem/2026-08-24.md | head -20",l:"диагностика"},{re:"^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Postmortem: incident severity SEV1 в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-29","Velero: schedule pause/unpause","Middle",`<h3>Контекст</h3><p>SRE DR: <b>Velero: schedule pause/unpause</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero: schedule pause/unpause</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero backup get | grep -A2 prod-daily",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero backup create prod-manual --include-namespaces prod --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero backup get | grep -A2 prod-daily",l:"диагностика"},{re:"^velero backup create prod-manual --include-namespaces prod --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero: schedule pause/unpause в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-30","etcd: quorum 2/3 lost","Senior",`<h3>Контекст</h3><p>SRE DR: <b>etcd: quorum 2/3 lost</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>etcd: quorum 2/3 lost</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",`DB SIZE 2.1GB`,"warn"],["^ETCDCTL_API=3 etcdctl alarm list",`alarm:NOSPACE`,"warn"],["^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",`compacted revision 12345`,"ok"],["^ETCDCTL_API=3 etcdctl endpoint health",`healthy`,"ok"]],[{re:"^ETCDCTL_API=3 etcdctl endpoint status -w table | head -10",l:"диагностика"},{re:"^ETCDCTL_API=3 etcdctl compact $(ETCDCTL_API=3 etcdctl endpoint status -w json | jq -r '.[0].Status.header.revision')",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: etcd: quorum 2/3 lost в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-31","RTO: disaster recovery drill failed","Junior",`<h3>Контекст</h3><p>SRE DR: <b>RTO: disaster recovery drill failed</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>RTO: disaster recovery drill failed</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat docs/runbook/dr.md | grep -A2 RPO",`RPO 15m RTO 1h`,"warn"],["^velero backup get -o json | jq -r '.items[0].status.expiration'",`expiration 2026-08-25`,"warn"],["^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",`updated`,"ok"],["^cat docs/runbook/dr.md | grep RTO",`RTO 1h`,"ok"]],[{re:"^cat docs/runbook/dr.md | grep -A2 RPO",l:"диагностика"},{re:"^echo \"RPO 15m RTO 1h\" >> docs/runbook/dr.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: RTO: disaster recovery drill failed в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-32","SLO: SLI vs SLO vs SLA","Middle",`<h3>Контекст</h3><p>SRE DR: <b>SLO: SLI vs SLO vs SLA</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>SLO: SLI vs SLO vs SLA</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat slo.yaml | grep -A2 burnRate",`burn rate 14x`,"warn"],["^promtool query instant http://prometheus:9090 'burn_rate'",`14`,"warn"],["^kubectl apply -f slo-fix.yaml",`applied`,"ok"],["^cat slo.yaml | grep -A2 target",`target 99.9`,"ok"]],[{re:"^cat slo.yaml | grep -A2 burnRate",l:"диагностика"},{re:"^kubectl apply -f slo-fix.yaml",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: SLO: SLI vs SLO vs SLA в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-33","Error budget: policy 50% halt","Senior",`<h3>Контекст</h3><p>SRE DR: <b>Error budget: policy 50% halt</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Error budget: policy 50% halt</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat error-budget.json | jq .remaining",`remaining 2%`,"warn"],["^cat slo.yaml | grep -A3 errorBudget",`errorBudget 100%`,"warn"],["^echo \"freeze release\" >> error-budget.json",`freeze`,"ok"],["^cat error-budget.json | jq .policy",`policy 50% halt`,"ok"]],[{re:"^cat error-budget.json | jq .remaining",l:"диагностика"},{re:"^echo \"freeze release\" >> error-budget.json",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Error budget: policy 50% halt в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-34","Postmortem: follow-up tickets","Junior",`<h3>Контекст</h3><p>SRE DR: <b>Postmortem: follow-up tickets</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Postmortem: follow-up tickets</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^cat postmortem/2026-08-24.md | head -20",`# Incident 2026-08-24 2h`,"warn"],["^cat postmortem/2026-08-24.md | grep -i \"Action Items\"",`no action items`,"warn"],["^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",`added`,"ok"],["^cat postmortem/2026-08-24.md | grep -c \"Action\"",`3`,"ok"]],[{re:"^cat postmortem/2026-08-24.md | head -20",l:"диагностика"},{re:"^echo \"- [ ] fix alarm\" >> postmortem/2026-08-24.md",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Postmortem: follow-up tickets в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
S("SRE DR","gc-dr-35","Velero: velero backup describe --details","Middle",`<h3>Контекст</h3><p>SRE DR: <b>Velero: velero backup describe --details</b>. Работа с <code>sre/config.yaml</code>.</p><h3>Что происходит</h3><p>Симптом: <b>Velero: velero backup describe --details</b>. Файл <code>sre/config.yaml</code> содержит ошибку.</p><h3>Что нужно сделать</h3><ul><li>[ ] диагностика</li><li>[ ] исправить</li></ul><h3>Ограничения</h3><p>Меняйте только <code>sre/config.yaml</code>.</p><h3>Стартовое состояние</h3><p>Файлы: <code>sre/config.yaml</code>. Активный: <code>sre/config.yaml</code>.</p><h3>Ожидаемый результат</h3><p>Чек-лист зелёный.</p><h3>Проверка</h3><pre>cat sre/config.yaml</pre>`,"dev@sre:~$",[["^velero backup get | grep -A2 prod-daily",`prod-daily PartiallyFailed 2d ago`,"warn"],["^velero backup describe prod-daily --details | grep -A2 Status",`Status Failed`,"warn"],["^velero backup create prod-manual --include-namespaces prod --wait",`Backup completed`,"ok"],["^velero backup get | grep Completed",`Completed`,"ok"]],[{re:"^velero backup get | grep -A2 prod-daily",l:"диагностика"},{re:"^velero backup create prod-manual --include-namespaces prod --wait",l:"исправить"}],{file:"sre/config.yaml",files:{"sre/config.yaml":`# SRE DR broken\nstatus: broken\n`},checks:[{re:/ok/,l:"ok"}],solutionFiles:{"sre/config.yaml":`# SRE DR fixed\nstatus: ok\n`}} ,{hints:["Симптом: Velero: velero backup describe --details в sre/config.yaml. Ищи причину в этом файле.","Открой sre/config.yaml в редакторе. Начни с cat sre/config.yaml.","диагностика → исправить"]});
